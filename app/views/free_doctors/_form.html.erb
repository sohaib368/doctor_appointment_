<%= form_with(model: free_doctor, local: true) do |form| %>
  <% if free_doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(free_doctor.errors.count, "error") %> prohibited this free_doctor from being saved:</h2>

      <ul>
        <% free_doctor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
